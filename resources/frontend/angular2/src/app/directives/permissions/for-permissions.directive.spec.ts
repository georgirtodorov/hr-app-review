import { ForPermissionsDirective } from './for-permissions.directive';

describe('ForPermissionsDirective', () => {
  it('should create an instance', () => {
    const directive = new ForPermissionsDirective();
    expect(directive).toBeTruthy();
  });
});
